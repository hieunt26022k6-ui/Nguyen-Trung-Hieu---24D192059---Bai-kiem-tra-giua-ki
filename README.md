# Bài kiểm tra giữa kỳ PHP - Lớp 02

Họ và tên: Nguyễn Trung Hiếu - 24D192059

## Nội dung

| File | Nội dung |
|---|---|
| `TracNghiem.txt` | Phần 1 - Trắc nghiệm: Câu 1 ghi kết quả in ra màn hình, Câu 2-5 chọn đáp án |
| `Bai1_TuLuan.php` | Bài 1 - Hàm `generateFibonacci($n)` trả về dãy Fibonacci, hiển thị 10 phần tử đầu tiên |
| `Bai2_TuLuan.php` | Bài 2 - Quản lý học sinh bằng mảng kết hợp, hiển thị danh sách và tìm học sinh điểm cao nhất |

## Cách chạy

Đặt thư mục này vào `htdocs` của XAMPP rồi mở trên trình duyệt:

```
http://localhost/Midterm-file/Bai1_TuLuan.php
http://localhost/Midterm-file/Bai2_TuLuan.php
```

## Ràng buộc kỹ thuật

Chỉ dùng hàm và cú pháp đã học ở buổi 01-06: `array()`, mảng kết hợp, mảng đa
chiều, `for`, `foreach`, `if`, `++`, `echo` nối chuỗi bằng dấu `.`, hàm tự định
nghĩa có type hint tham số. Không dùng hàm có sẵn nào ngoài các cấu trúc trên.

Đã kiểm tra bằng PHP 8.3.33: lint, chạy CLI và chạy qua trình duyệt - không có
warning hay error.
